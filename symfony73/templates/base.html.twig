<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        {% block stylesheets %}
         <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-light">
    <nav class="navbar navbar-expand-sm bg-primary navbar-dark">
  <div class="container-fluid">
    <ul class="navbar-nav">
    
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.budgets2',{'year':'2025','budget':'juin2025'})}}">Budgets</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.home')}}">Comptes</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.create')}}">Newop</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.virement')}}">Virement</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.cheques')}}">Cheques</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.cbid2',{'slug':'0380634'})}}">CBid</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('euros.libelles2',{'slug':"tchip\ coiffure"})}}">Libelles</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('app_actifs_index')}}">Actifs</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('app_task')}}">Taches</a></li>
      <li class="nav-item"><a class="nav-link active" href="{{path('app_jobs_index')}}">Jobs</a></li>
      
  </div>
  
  {% set mois = ['janvier', 'fevrier'] %}
  {% set zz=1 %}
<span class="text-white"> xx {{mois.1}} yy</pan>
  <span class="text-white"> {{ "now"|date("Y") }}    -- </span>
</nav>

    <div class="container my-4">
    
    {% include 'partials/flash.html.twig' %}
    
        {% block body %}{% endblock %}
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
