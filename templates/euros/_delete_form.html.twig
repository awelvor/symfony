<form method="post" action="{{ path('app_euros_delete', {'id': euro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ euro.id) }}">
    <button class="btn">Delete</button>
</form>
